# AKS Manifests

Supporting artifacts referenced by the AKS how-to guides.

- `disable_kube_proxy_byocni.json`: Example Azure CLI extension payload that disables kube-proxy when supplying your own CNI plugin.

Apply or pass these files exactly as referenced in the matching how-to markdown to keep the walkthroughs reproducible.
