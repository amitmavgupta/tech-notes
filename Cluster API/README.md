# Cluster API

Artifacts for provisioning Kubernetes clusters via Cluster API on AWS and Azure.

## Contents
- `AWS/`: IAM roles and workload cluster manifests.
- `Azure/`: CAPZ sample cluster spec plus helper scripts.

Use the subdirectories that match your target cloud when initializing Cluster API management clusters.
