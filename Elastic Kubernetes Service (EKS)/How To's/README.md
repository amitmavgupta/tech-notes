# EKS How To's

Step-by-step guides for common EKS operational tasks.

## Topics
- Cluster lifecycle management (`eksctl-get-cluster.md`, `nodegroup-addition-using-eksctl.md`).
- Upgrades and maintenance (`ekskernelupgrade.md`, `upgrade_eks_cluster_to_pod_identity.md`).
- Observability and tuning (`eks-coredns-autoscaling.md`, `eks-logging.md`).

Follow the instructions sequentially; commands are written for `eksctl`, `kubectl`, and AWS CLI.
