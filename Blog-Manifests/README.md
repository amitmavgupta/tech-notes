# Blog Manifests

Manifests, Dockerfiles, and markdown notes referenced in blog posts and demos.

## Highlights
- Private Link and network policy samples for AKS (`network_policy_private_link_service_aks.yaml`, `trusted_application_aks.yaml`, etc.).
- HPC and AI workloads such as `mpi-job-manifest_eks.yaml` and `nccl-tests.Dockerfile`.
- Platform notes including `eks_trunk_interface.md` and `gke-kube-proxy-removal-manifest.md`.

Use these files as copy/paste-ready artifacts while writing or presenting blog content.
